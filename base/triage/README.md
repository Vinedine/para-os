# triage

Landing zone for anything that doesn't obviously fit elsewhere. Drop new files here, then sort each into its PARA home at the next review: a contact into `areas/network/`, project work into `projects/`, reference into `resources/`, a dated record into `archive/meetings/`.

<!-- Placeholder so the folder exists in a fresh vault. Delete once real content lands. -->
