# projects

One folder per time-bound piece of work with a committed deliverable or deadline. Each project folder holds a `brief.md`, an `actions.md`, and, where relevant, a `sources/` subfolder.

<!-- Placeholder so the folder exists in a fresh vault. Delete once the first project lands. -->
