# {{Vault Name}} Vault Conventions

{{One-line description of what this vault covers.}} Per-vault guidance for Claude Code sessions.

<!-- OPTIONAL: add a "## Context" section if this vault needs scope boundaries or cross-vault
     context (e.g. "building ownership is documented in the X vault; this vault only covers
     operations"). Delete this comment if not needed. -->

## PARA layout

- **triage/** - landing zone for anything that doesn't obviously fit elsewhere; emptied via `/para-triage` at the next review.
- **projects/** - {{what a "project" means here. Default: time-bound work with a committed deliverable and deadline, one folder per project, each holding a `brief.md` + `actions.md` and, where relevant, a `sources/` subfolder.}}
- **areas/** - ongoing responsibilities. `business/` = running narrative and strategic actions. `network/` = one file per person.{{ Add any other area subfolders this vault needs.}}
- **resources/** - reusable reference. `prompts/` = reusable specs and drafts. `ideas/` = concept-stage ventures with no committed outcome; same `brief.md` shape as a project for easy promotion.
- **archive/** - inactive artifacts. `meetings/` = dated meeting/chat/email records.{{ Add `projects/` = completed work and `ideas/` = shelved concepts, if this vault archives finished projects and retired ideas (both filled by `/para-archive`).}}

Promote `resources/ideas/<x>` to `projects/` when one of: someone is waiting on a deliverable by a date; money or a formal engagement is committed; a go/no-go review is on the calendar. Ideas with no traction at 6+ months retire to `archive/ideas/` (via `/para-archive`).

Root `README.md` is the master document, single source of truth for identity, operating model, and track record. Derived outputs regenerate from it.

### Archive hygiene

What a clean `archive/` looks like (applied on the way in, and audited by `/para-deep-clean`):

- **No live work in the archive.** An archived entity has zero open actions - every item done, or open items softened to an explicit "fully closed; minor gaps not material" note. Surviving work is routed out *before* archiving (to a successor idea, an existing project, or `areas/business/actions.md`), never left as a dangling `[ ]`. A closed project must not keep surfacing in `/para-daily-brief`.
- **History archives; living references go to `resources/`.** Brief, actions, and one-time migration/handoff notes are history - they archive with the entity. Reusable material (playbooks, positioning/strategy docs, templates, anything other live work links to as a resource) is *not* buried in the archive; route it to `resources/<name>/`.
- **No loose files at the archive root.** Everything under `archive/` lives in a subfolder (`meetings/`, `projects/`, ...). A stray top-level file is misfiled.
- **Minimum record.** Each archived entity carries a status marker (why it's archived) and a `brief.md`/`README.md`, so it's self-explanatory years later.
- **Zero dangling links.** Moving something into the archive repoints every inbound reference (contacts, areas, README, other entities) in the same pass; a stale link is rot. A historical mention *inside* the archived folder itself is fine.

## Filing rules

- **Contacts**: one file per person at `areas/network/<firstname-lastname>.md` (kebab-case, no diacritics). Relationship context at the top, `## Next actions` appended at the bottom.
- **Actions**: co-locate with the project, idea, or contact they belong to. Per-project actions live in `projects/<project>/actions.md`; per-idea actions in `resources/ideas/<idea>/actions.md`; per-contact actions inside the contact file. Strategic work not tied to one of those goes in `areas/business/actions.md`. Never create a monolithic root `actions.md`.
- **Dated meeting/chat/email artifacts**: `archive/meetings/YYYYMMDD Description.ext`.
- **Source documents**: {{the naming convention for filed documents, e.g. `YYYYMMDD <Who> <Description>.<ext>`. The /para-triage skill applies this convention literally; if you leave it undefined, /para-triage will ask before renaming anything.}}
- **Shared workstreams across contacts**: track actions in the primary contact's file; the other files get a pointer line.
- **Project brief + relationship**: the contact file is the relationship summary; the project folder holds the brief and execution detail. Cross-link both ways.

## Obsidian Tasks markers for actions.md files

Any `actions.md` file in this vault uses Obsidian Tasks plugin markers. The `/para-daily-brief` skill parses these with regex:

- `📅 YYYY-MM-DD` - due date (hard deadline)
- `🛫 YYYY-MM-DD` - start date (item not actionable until then)
- `⏳ YYYY-MM-DD` - scheduled (planned work date)
- `🔁 every <cadence>` - recurring (pair with `📅` for the next occurrence)
- `🔺 🔼 🔽 ⏬` - priority, highest to lowest. Medium = no marker. Use 🔺 sparingly.
- `✅ YYYY-MM-DD` - completion (auto-filled when ticked)

Rules when writing or editing tasks:

- Put markers at the end of the line: `- [ ] Pay invoice 📅 2026-05-15`
- Replace free-text dates with markers (no "before Jul 10 📅 2026-07-10", just the marker)
- Convert fuzzy dates ("Q4", "summer") to concrete dates (usually end-of-quarter or end-of-season)
- For items gated on external events, leave undated. Do not invent dates.
- One flattened `## Recurring` section per file, not separate "quarterly" / "annually" headings.

Never archive an `actions.md` file: it must stay outside `archive/` to be picked up by `/para-daily-brief`.

## Language

Folders and structural files in English. Content (notes, contacts, briefs) may be in any language, matching the source. Don't translate unless asked.

## Memory

This vault on disk IS the memory. Do not use the agent's built-in memory feature, and do not create a `memory/` folder or session-log files (`/AI/sessions/` and the like). The agent reads the vault fresh each session; the git history is the audit log. Durable facts belong in the file they describe: conventions in this `CLAUDE.md`, identity and operating model in `README.md`, and everything else in the relevant project, idea, or contact note. If you would otherwise save a memory, write the fact into the vault file it belongs to instead.

## File formats

The substrate is real, readable files. The agent works over what exists on disk as bytes, so prefer formats it can read and move directly: Markdown and plain text first (read and edited in place, and they diff cleanly in git), with `.csv`/`.docx`/`.xlsx` as the fallback for material received from others. Avoid cloud-native pointer formats (e.g. Google Docs/Sheets): they sync as stubs linking to content held on a server, so the bytes aren't local, tooling can't move or rename them, and the agent often can't read them at all. When one lands in the vault, convert it to a real file. A format the tooling can't touch is dead weight, the same way a junk folder is.

## Do not add

- **Templates** - do not create per-contact or per-project templates preemptively. Wait until a second instance proves the shape, then document it here.
- **Derived outputs as standalone files** - web copy, deck content, one-pager text regenerate from `README.md`. Do not let them drift into separate source-of-truth files.
- **Content rewrites during reorganization** - structure changes preserve copy verbatim. Fix wording in a later, dedicated pass.
- {{Anything else that belongs in a different vault or system, e.g. "Accounting, invoicing, financial records - belong in a separate vault (one vault per life context)."}}

<!-- OPTIONAL sections that mature vaults grow - add only when this vault actually needs them:
  - "## MCP integration intent" - which connectors (MCP servers) this vault uses and for what, one
    bullet per server with its scope. The shared rule across all of them: fetch tickets / pages /
    repo contents on demand; never mirror whole tickets, pages, or repos into the vault. Omit the
    section entirely if the vault has no MCP configured.
  - "## Operating rules" - vault-specific working rules that aren't structure: drafting register for
    a given recipient, read-only-connector restraints, domain naming conventions, deliverable style.
    Each is local to this vault; do not import another vault's rules.
  - "## README structures" - canonical section order per entity type, ONLY once a SECOND instance
    of that entity type proves the shape.
  - "## Cross-vault references" - when this vault overlaps another. Keep the full entry where the
    relationship is primary and a short pointer line in the other.
  - "## Project context" - durable, non-obvious facts about ongoing work not derivable from the files. -->
